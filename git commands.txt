git init

git status

git add file1.txt
git add .

// remove all added files
git git remove --cached -r .

git commit -m "complete first stage"

git log

//to get the previous file data if something went wrong
git diff file1.txt 

// to rollback to previous version of a file
git  checkout file1.txt

// add your diroctory code to a directory on github
git remote add origin https://github.com/upside-down-code/story.git

// push your code on github
git push -u origin master

// clone a git project 
git clone https://github.com/RyanFitzgerald/devportfolio.git



